//- the_post_navigation
.
  <?php
    if ( !function_exists( '#{postNavigation}' ) ) {
      function #{postNavigation}() {
        $prev_post = get_previous_post();
        $next_post = get_next_post();
        if($prev_post){
  ?>
a(href!="<?php echo get_permalink($prev_post->ID); ?>" class="prevPost")
  h4 << <?php echo $prev_post->post_title; ?>
  //- <?php if(wp_get_attachment_url(get_post_thumbnail_id($prev_post->ID))) { ?>
  //- img(src!="<?php echo wp_get_attachment_url(get_post_thumbnail_id($prev_post->ID)); ?>" alt!="")
  //- <?php } ?>
  <?php } ?>
<?php if($next_post){ ?>
a(href!="<?php echo get_permalink($next_post->ID); ?>" class="nextPost")
  h4 <?php echo $next_post->post_title; ?>
    //- | <?php if(wp_get_attachment_url(get_post_thumbnail_id($next_post->ID))) { ?>
    //- img(src!="<?php echo wp_get_attachment_url(get_post_thumbnail_id($next_post->ID)); ?>" alt!="")
.
  <?php
          //- }
        }
      }
    }
  ?>